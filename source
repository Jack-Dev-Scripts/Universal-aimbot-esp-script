-- JACKDEV BASIC UI PATCH
-- Replace your current GUI section with this simpler layout.

local Colors = {
    Background = Color3.fromRGB(35,35,35),
    Panel = Color3.fromRGB(45,45,45),
    Button = Color3.fromRGB(60,60,60),
    Accent = Color3.fromRGB(0,170,255),
    Text = Color3.fromRGB(255,255,255),
    SubText = Color3.fromRGB(180,180,180),
}

-- Smaller window
Main.Size = UDim2.fromOffset(560,380)
Main.Position = UDim2.new(0.5,-280,0.5,-190)
Main.BackgroundColor3 = Colors.Background

TopBar.BackgroundColor3 = Colors.Panel
Sidebar.Size = UDim2.fromOffset(130,300)
Content.Size = UDim2.fromOffset(410,300)

-- Simple card layout
Head Aimlock   (Y = 0)
Chest Aimlock  (Y = 60)
Player ESP     (Y = 120)
Keybinds       (Y = 180)
FOV            (Y = 240)
Smoothness     (Y = 300)

-- Use plain gray buttons with blue ON state.
-- Use simple text labels (no gradients or glow).

-- Head / Chest lock logic:
Settings.LockPart = "Head"

-- Head button:
Settings.LockPart = "Head"

-- Chest button:
Settings.LockPart = "Chest"

-- If Head is selected, Chest turns OFF.
-- If Chest is selected, Head turns OFF.
